<%= form_for @task do |t|%>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_area :details %>
  <%= t.check_box :completed %>
  <%= t.label :completed, 'Completed' %>
  <%= t.submit %>
<% end %>

<%= link_to 'Back to list', tasks_path %>
